// Observability — Prometheus /metrics endpoint
// See: FR-015 AC-015-01, docs/requirements/REQ-GH-263-.../module-design.md §Module 14

/**
 * Prometheus text-format metrics handler.
 * @returns {Promise<string>}
 */
export default async function metricsHandler() {
  throw new Error('Not implemented — see T028');
}
