// Observability — OpenTelemetry OTLP exporter wiring
// See: FR-015 AC-015-03

/**
 * Initialise OpenTelemetry tracing if OTLP_ENDPOINT is configured.
 * @param {object} config
 * @returns {Promise<void>}
 */
export default async function initTracing(config) {
  throw new Error('Not implemented — see T028');
}
